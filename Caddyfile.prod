{$DOMAIN:localhost} {
    handle /uploads/* {
        root * /srv
        file_server
    }

    handle {
        reverse_proxy app:3000
    }
}

www.{$DOMAIN:localhost} {
    redir https://{$DOMAIN}{uri} permanent
}
